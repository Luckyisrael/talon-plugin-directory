# panta-api

Talon plugin pack: Panta API integration patterns.

**Status: structure published, skill data in authoring.** The manifest,
hashes, and registry row are real and verified; skill bodies and
`references/` starting points arrive with the API details. A skill that
still says "data pending" must never be built against — it says so in its
own body.

See the root [README](../../README.md) for how plugins load.
